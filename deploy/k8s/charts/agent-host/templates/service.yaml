apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-host.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-host.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agent-host.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
