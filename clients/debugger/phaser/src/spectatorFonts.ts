/**
 * Canvas text stack for the spectator client: Noto Color Emoji first so emoji render reliably in
 * Phaser `Text` objects (see `@fontsource/noto-color-emoji` imported from `main.ts`).
 */
export const SPECTATOR_UI_FONT_FAMILY =
  '"Noto Color Emoji", ui-monospace, Menlo, Monaco, "Courier New", monospace';
