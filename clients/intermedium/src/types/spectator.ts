/** Colyseus + map load status for the spectator shell (US1+). */
export type ColyseusLinkState = "disconnected" | "connecting" | "reconnecting" | "connected";

export type MapGramStatus = "idle" | "loading" | "ready" | "error";
