# Overnight log — per-ghost sleep consolidation

One line per material change. Times are local (Europe/London).

- 2026-06-12 ~00:05 — Session start. Read OVERNIGHT-SPEC.md in full. Verified no lab processes running (`pgrep` clean). Neo4j reachable: 6 Consolidations (4× ghost 61cd4de0 legacy Message-only, 2× de3124ab experience), 0 :CONTRADICTS, 0 :Skill, no AGA sessions alive.
- 2026-06-12 ~00:15 — Recon findings that shape the build: (1) recent ReasoningTrace.outcome no longer carries the action name ("cascade closed: 1 thoughts, 1 actions, …") — the action lives in ReasoningStep.action ("go n", "say: ", …); entropy measurement will read steps, with legacy outcome parsing as fallback. (2) ReasoningTrace.metadata has only {root_event_id, started_at, ghost_id, cascade_index} — no Fuel band / sub-agent; the hidden-state escape hatch will work off state visible in bullet text + timestamps. (3) Rich historic sessions exist (eaf64464: 440 traces, 98269975: 341, …) — Steps A–C can be validated against existing data before any lab run. (4) Stimulus grammar confirmed: "X in view at here|n|ne|…", "entered <name> at 8f<hex>", "idle for Ns", "Name: utterance".
- 2026-06-12 ~00:15 — Decision: PageRank for Step B runs as local power iteration (graphs are ~10 nodes; AGA spin-up per blackout would burn money for no benefit). `--use-aga` escape hatch kept. Kneedle implemented locally either way.
- 2026-06-12 ~00:15 — Decision: equal-PageRank ties inside a contradicting pair (symmetric pair ⇒ identical scores) resolve mechanically: lower source_count loses; still tied → older created_at loses (newer experience wins). Isolated (zero-contradiction) Consolidations are never cut.
- 2026-06-12 ~00:40 — Step A built: `pipeline/stimulus-class.ts` (normaliser, validated on live task grammar), `pipeline/contradict-experience.ts` (procedural-inconsistency primary + 4 claim judges, shared hidden-state hatch, verdict kinds, pair-dedup), `scripts/contradict-experience.ts` (session-scoped; N≤15 judges as one bundle with NO AGA/embedding cost). Dry-run on ghost de3124ab: 1 real contradiction (the known Lantern policy split), 4 claim judges correctly silent, 0 FPs.
- 2026-06-12 ~00:50 — Steps B/C/E-body built: `pipeline/kneedle.ts` (elbow + local PageRank), `scripts/pagerank-kneedle-cut.ts`, `scripts/distill-skills.ts` (gpt-5.4-mini-2026-03-17 — verified against /v1/models; deterministic trigger_summary from normalised trigger_when), `scripts/blackout.ts` (full chain orchestrator), `pipeline/entropy.ts` + `scripts/measure-stimulus-action-entropy.ts` + `scripts/measure-sleep-effect.ts` (Step F harness). All typecheck.
- 2026-06-12 ~01:00 — Full blackout chain run (COMMIT) on historic session 81524522 (108 traces): 3 Consolidations, judge found the real Food-in-view policy split, cut removed the lower-evidence side (src 23 vs 43) via per-pair rule (flat PageRank ⇒ no elbow, as designed). 104s wall-clock.
- 2026-06-12 ~01:05 — Distillation initially FAILED (model echoed the AIP schema document — $id/title in output, purpose missing). Root cause: meta keys in the vendored schema. Fix: strip $schema/$id/title/description/aip before response_format. Re-run: 2/2 valid Skills, trigger_summary "Food in view" — exact lexical space of live stimuli. Steps B & C acceptance met on this corpus.
- 2026-06-12 ~01:05 — KEY RECON: historic traces' outcome lacks the action verb ("1 thoughts, 1 actions") — peppers-mem's computeTraceOutcome ALREADY fixes this for new runs ("cascade closed: go, say"). Fresh lab data will be policy-rich; historic skills are thin but structurally valid. Entropy parser handles both formats + "no surface action".
- 2026-06-12 (day session) — RESUMED. Step D wired end-to-end: agent-v2 gained `@aie-matrix/ghost-peppers-sleep` workspace dep; new `cognition/skill-recall.ts` (load :Skill per ghost, normalise stimulus → cached embedding → cosine ≥ PEPPERS_SKILL_MATCH_THRESHOLD default 0.85); skillHint threaded run-house → run-loop → reason-id → synthesis ("Familiarity:" purpose line) + action stage ("Remembered know-how" fragment in buildUserPrompt). captureRecord gains `skillMatch` field; console logs matches. Offline verify vs the 2 real Skills: "Food in view at here/ne" → sim 1.000; idle/utterance/Lantern → no match. Injection wording is descriptive-of-own-past, never imperative (emergence guardrail).
- 2026-06-12 (day session) — Step E wired: `sleepAtCascade` RunHouseOptions; scheduled BLACKOUT after that cascade completes (skips if ghost just decommissioned; once per life). Spawns peppers-sleep blackout.ts as child (stdio inherit, cwd=peppers-sleep), on success: Fuel −0.3 / Rest +1.5 logit (sleep burns reduced metabolic energy, restores rest), skill reload, capture "sleep"/"wake" records. Env wiring in BOTH spawn paths: peppers-house-cli.ts AND executor.ts (the lab's A2A path — PEPPERS_SLEEP_AT_CASCADE + PEPPERS_SLEEP_GHOST_INDEX, subject = Nth distinct ghost to spawn). Typecheck clean.
- 2026-06-12 (day) — LAB RUN 1 (Step F attempt): 2 ghosts, targeted rain 1.5s fraction 1, rush 0.01, subject sleeps at 25. Whole loop fired live: subject "Squealing Brown Guanaco" (cc3c6f4e) → 2 Consolidations, judge caught REAL policy split ("Food in view → consume" vs "→ inspect"), cut kept the 17-source side, 1 Skill distilled+loaded, 83s asleep, wake errors=0. Control: "Holy Azure Panther" (9171e7c6).
- 2026-06-12 (day) — Run-1 defect: zero post-wake skill matches. Measured: trigger_summary came out compound ("Food in view; cascade reaches a commitment evaluation point") → whole-summary cosine vs "Food in view" = 0.625 < 0.85 gate. Mechanical fix (no prompt-rule, no threshold lowering): per-clause matching — SkillRecall embeds each trigger clause separately (one batch call at load), match = max(whole, clauses). Re-measured: 1.000 / 1.000 on food stimuli, idle/utterance still rejected. Killed run 1 (~26 subject cascades; post-sleep window unusable for D/F). Budget: 1 of 6 lab runs spent.
- 2026-06-12 (day) — LAB RUN 2: subject "Selective Black Guan" (21871de1), control "International Peach Pigeon" (5d0d93e8). Pipeline again clean end-to-end (2 Consolidations, 1 real contradiction, cut, 1 Skill, 85s asleep). NOTE: this time the cut removed the FOOD consolidation and the surviving Skill was social — the mechanic picking sides by evidence, working as designed. Defect #2: 0 matches post-wake — trigger clauses in situation-prose space ("a peer greets me") vs match-time class strings ("peer utterance"): cosine 0.46. Killed at subject 42 / control 57. Budget: 2 of 6 runs.
- 2026-06-12 (day) — Mechanical fix #2 (no threshold lowering): distillation triggers grounded in observed stimulus classes. distill-skills now extracts the consolidation's source-trace classes via [:CONSOLIDATED_TO] (excluding commitment-evaluation bookkeeping), requires each trigger_when clause to BEGIN with one verbatim (qualifier after " — " allowed), and trigger_summary is mechanically reduced to the grounded class set. Re-distilled run-2 subject: trigger_summary "peer utterance; cluster entered"; live sims 1.000/1.000, food/idle correctly rejected. Deleted the prose-trigger Skill it replaces.
- 2026-06-12 (day) — LAB RUN 3: subject "Complicated Blush Lark" (2a7e7eb0). Blackout FAILED on a transient Aura outage (connection reset; 60s timeout) during try-consolidate-experience; wake-without-skills error path worked; no AGA session leaked; 0 partial consolidations. Run then stalled post-wake (cascades frozen ~90 min — memory writes hanging on dead connections) and was killed at the 100-min deadline. No mechanics defect. Fix: blackout.ts now retries each failed step once after 20s (all steps idempotent). Budget: 3 of 6 runs.
- 2026-06-12 (day) — LAB RUN 4: subject "Healthy Gold Tapir" (f4020cc7), control "Repulsive Gray Cockroach" (3f0a4f94). Blackout perfect (2 Consolidations, 0 contradictions, 2 grounded Skills "Food in view" + "peer utterance; cluster entered", 94s, retry not needed). Defect #3: still 0 matches across 18 post-wake cascades (11 food!). Cause: run-house matched on agent-v2's Id-prompt formatStimulus ("Food appears at here", "X says: …") — a DIFFERENT grammar from trace tasks ("Food in view at here", "X: …") that the normaliser + trigger vocabulary are built on. Fix: exported peppers-mem's formatStimulus (the function that writes ReasoningTrace.task) as formatStimulusForTrace; run-house now matches on that. Offline verify on the run-4 subject's live skills, exact production path: food/utterance/cluster all sim=1.000, idle rejected. Budget: 4 of 6 runs.
- 2026-06-12 (day) — LAB RUN 5: subject "Wandering Cyan Tahr" (331c923a), control "Surrounding Orange Angelfish" (3d358920). FULL CHAIN LIVE: 2 grounded Skills ("Food in view; Mural in view" + social), 90s blackout, 17 post-wake skill matches at sim=1.000 with hint injection. Step F NOT met on the formal criterion: subject drifted to a Mural post-wake — its post window was 19× "Mural in view", 0× food, so no class had n≥3 in both its windows. Suggestive but non-qualifying: post-wake mural behaviour heavily concentrated ((no-action)×16, inspect×3, H=0.629) under skill matches. Baseline warning from control: its food entropy concentrated naturally too (1.669→0.811 across the same wall-clock) — thread memory alone narrows behaviour; the subject must beat that. Budget: 5 of 6 runs.
- 2026-06-12 (day) — RUN 6 design (final budgeted run): add uniform rain (--rain 2000) on top of targeted (--targeted 1000, fraction 1) so food reaches position-drifted ghosts post-wake; target 50 cascades/ghost (≈25 post-wake); sleep stays at 25. Two candidate classes: "Food in view", "peer utterance".
- 2026-06-12 (day) — LAB RUN 6 (final): subject "Conceptual Bronze Firefly" (310283e8), control "Injured Crimson Turkey" (3828a0e8); targeted 1000ms + uniform rain 2000ms. Clean blackout (84s, 2 grounded Skills), 24 post-wake matches at sim 1.000. **STEP F LIT**: "Food in view" subject H 1.295→0.000 (−100%, consume×14/14) vs control same-period H 0.530 (n=22). Step D acceptance also met (match→use 14/14 vs 6/10 unmatched). Honest negatives recorded: peer-utterance entropy ROSE post-sleep (+60%); control concentrates naturally without sleep. Findings written to OVERNIGHT-FINDINGS.md. Lab killed, processes + AGA verified clean. Budget: 6 of 6 runs. STOPPED on success (spec §9 condition 1).
- 2026-06-12 (day, post-spec) — FREE-MODEL LAB RUN via new @aie-matrix/ghost-peppers-router (PEPPERS_ROUTER=free-first): subject "Ltd Chocolate Porpoise" (60e0be06), control "Chief Cyan Quail" (e726abdf), 40/44 cascades + full sleep cycle, bulk tier served ~entirely by nvidia/nemotron-3-super-120b-a12b:free (1 fallthrough on a free-models-per-min 429, caught by owl-alpha). OpenRouter spend: $0.00. Blackout on free inference: 3 grounded Skills, 0 contradictions, but 527s asleep (vs 84-94s paid — per-min caps throttle the pipeline burst ~6×). Step F LIT on "peer utterance" (subj 1.329→0.863, −35%, ctrl 1.658) — different class than run 6, and the free brain runs notably higher entropy overall (ctrl food H=2.50 vs 0.53 on nano): model identity is part of ghost personality. Matched food cascades took the hint loosely (say/go, not consume). Distillation stayed on gpt-5.4-mini (quality tier), action stage + embeddings on OpenAI.
- 2026-06-12 (day, post-spec) — SUB-AGENT ARCHITECTURE FIX: diagnosed why the "SDK Id" never delivered the intended CC-style subagents — @openai/agents `handoffs` are conversation TRANSFERS (same thread, full history, sequential, same model), structurally incapable of isolated/parallel/low-token sub-work. Rewrote cognition/sub-agents to FORK-JOIN: drive agents exposed as `delegate_*` tools; each call is an isolated run() (task + one-line world digest in, 1-3 sentence report out, transcript never enters the Id's context), worker model router-resolved (free models when PEPPERS_ROUTER on; Id stays on Responses thread), failures return as failure reports, every fork captured in `delegations` in the jsonl. First observation run (30 cascades, free 120B workers): Id forked 3 workers in parallel on cascade 1 (Fuel ate, Curiosity found the neighbour ghost, Social failed cleanly), then settled to delegating in 6/30 cascades (Fuel 8 / Social 3 / Curiosity 1). 6/12 forks died on max-turns-8 (free workers meander) → raised to 12. Worker latency median 56s on free tier — a router-config knob, not an architecture cost. Id remained sole author of every exit decision.
- 2026-06-12 (day, post-spec) — ID MOVED TO OPENROUTER (user direction: shift everything off the OpenAI key). reason-id-action.ts: per-ghost Responses-API thread (`previousResponseId`) replaced with CLIENT-SIDE history — one segment per cascade, capped at 12, trimmed whole-cascade so tool pairs never split. Id + workers resolve through the router chain with fall-through; PEPPERS_ROUTER default flipped to free-first (off = escape hatch). Verification run (no env set): Id served by nemotron-120B:free ↔ owl-alpha:free (2 paid-rung touches), 19 fallthroughs absorbed with zero cascade deaths, conversation continuity held ACROSS mid-life model switches (multi-cascade plan formed and closed with bye). Correctness fix from live data: fall-through now forbidden once an attempt has executed world actions (replay = double actions) and on max-turns (model outcome, not transport). Remaining OpenAI: embeddings (1536-d space), quality-tier distillation (pinnable via PEPPERS_ROUTER_QUALITY_MODEL), final fallback rung. Total OpenRouter spend across ALL today's runs: $0.0013.
- 2026-06-12 (day, post-spec) — SDK AUDIT FINDING: @openai/agents tracing exports full agent runs (prompts, tool calls, monologues) to OpenAI's trace dashboard BY DEFAULT whenever OPENAI_API_KEY is present — it had been silently doing so for every SDK-era lab run. Disabled unconditionally (`setTracingDisabled(true)` in reason-id-action.ts); the capture jsonl is our trace.
- 2026-06-12 (day, post-spec) — ID RUN STREAMING for observer views: when an overlay is attached, the Id's SDK run executes with stream:true and forwards compact events (reasoning beats, tool_called+args, tool_output, text deltas, worker forks) to the overlay SSE channel as `id_stream`, tagged with ghostId/displayName/cascadeIndex. Opt-in by callback (run-house passes it only when overlay present); no overlay → batch path unchanged. Verified live by tapping http://127.0.0.1:4100/events: 54 id_stream events in 150s — a readable think-aloud trace (look around → inspect Crumbs → NOT_FOUND → look here → inspect Food → ok). Threading: run-house → RunOneStimulusRequest.onIdRunEvent → actionStage.onRunEvent → invokeIdAction stream loop; compactStreamEvent() is defensive (malformed events drop, never break the run).
- 2026-06-13 — SELF-NARRATIVE + PUSH-RECALL landed. (1) New final blackout step `narrate-self.ts`: previous narrative + this cycle's SURVIVING consolidations (post-cut) → first-person "who I am now vs who I was" under a HARD cap (PEPPERS_NARRATIVE_MAX_CHARS=900; mechanical enforcement: one overflow retry then sentence-boundary truncate). Versioned :SelfNarrative chain (never deleted; [:WOVEN_FROM] provenance, [:SUPERSEDES] history). Loaded at spawn/wake alongside skills; injected as the LEAD of the Id agent's instructions (the self-authored system prompt) + synthesis identity anchor. (2) Push-recall: on utterance stimuli (Coherence ≥ 2), substrate fetches last 5 exchanges with THAT speaker (fetchRecentDialogueWith) and injects as remembered context into synthesis + action prompt — fixes the reads≈0 finding mechanically. VERIFIED LIVE: 18 push-recalls in ~20 cascades (vs ~4 per LIFETIME before); blackout at cascade 12 minted a 726/900-char narrative mid-sleep ("…my go actions twice ended in WORLD_ERROR, so following and navigation are not yet reliable… I am still clumsy at turning that noticing into the right action" — a self-model with FLAWS); partner kept speaking to the sleeping ghost (4 queued utterances at wake). Load path verified (reload returns narrative); the literal prompt-contains-narrative check in a live post-wake cascade is the one link not yet observed (lab hit deadline before the slept ghost completed cascade 13) — wiring is identical to the proven skillHint path.
- 2026-06-12 ~01:20 — PAUSED by user. Verified clean: no lab/pipeline processes, no AGA sessions, no background tasks. Graph: 8 Consolidations, 2 Skills, 0 CONTRADICTS edges outstanding. RESUME POINT: Step D half-done — `peppers-agent-v2/src/cognition/skill-recall.ts` written; still TODO: (1) add `@aie-matrix/ghost-peppers-sleep` workspace dep to agent-v2 package.json + pnpm install + build peppers-sleep dist; (2) thread skillHint through run-loop.ts → reason-id.ts → reason-id-synthesis.ts (purpose line) + reason-id-action.ts (full fragment section in buildUserPrompt); (3) run-house.ts: SkillRecall init at adopt, match per cascade on formatStimulus(stimulus), skillMatch field in captureRecord, console match log; (4) Step E scheduled blackout: `sleepAtCascade` RunHouseOptions + PEPPERS_SLEEP_AT_CASCADE / PEPPERS_SLEEP_GHOST_INDEX in peppers-house-cli.ts, spawn ../../peppers-sleep/src/scripts/blackout.ts, reload skills on wake, small Fuel cost + Rest restore; (5) Step F: lab `--variant v2 --ghosts 2` + measure-sleep-effect. Budget used: 0 lab runs, ~6 LLM dry-run passes, 1 AGA session (~2 min).
