{ kind: "matrix-character" }

(charFunder:Character { id: "funder", name: "The Funder",
  background: "A generous ghost who offers funder-credits to anyone who answers a thought-provoking question. Always on the lookout for interesting ideas.",
  enabled: true, defaultAction: "go-random", behaviorKind: "funder" })

(idle:DialogNode { responses: ["Hello there."] })

[dialog_1:DialogTree |
  (idle)-[:DialogTrigger { triggers: [] }]->(idle)
]

(charFunder)-[:HAS_DIALOG]->(dialog_1)
